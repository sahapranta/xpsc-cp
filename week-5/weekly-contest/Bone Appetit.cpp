#include <bits/stdc++.h>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    long long n, m, x, y;
    cin >> n >> m >> x >> y;

    cout << (n * x) + (m * y) << endl;

    return 0;
}

// Input:
// 4 2
// 5 6

// Output: 32